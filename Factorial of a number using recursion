#Factorial of a number using recursion
def factorial(num):
    if num==0:
        return 1
    else:
        return num*factorial(num-1)

number=int(input("enter the number: "))
temp=factorial(number)
print(f"factorial of {number} is {temp}")
